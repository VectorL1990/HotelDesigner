using System;
using System.Collections.Generic;
using UnityEngine;


namespace CSG
{
    internal sealed class GenerateBox
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}
